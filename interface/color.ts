export interface IColor {
  id: string;
  name: string;
  colors: string;
}

export interface ColorProps {
  direction: string;
  colors: string;
  name: string;
  id: string;
}

export interface FavColor {
  id: string;
}
